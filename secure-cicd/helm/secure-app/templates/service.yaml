apiVersion: v1
kind: Service
metadata:
  name: secure-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: secure-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
