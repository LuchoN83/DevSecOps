apiVersion: v1
kind: Pod
metadata:
  name: "secure-app-test"
  labels:
    helm.sh/hook: test
spec:
  containers:
    - name: curl
      image: curlimages/curl
      command:
        - sh
        - -c
        - "curl -sSf http://secure-app:80/health | grep -q 'ok'"
  restartPolicy: Never
